@model ProjectMillenium.Web.Models.RoleViewModel
@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>

    <h3>Bu rolü silmek istediğinizden emin misiniz?</h3>
    <div>

        <hr />
        <dl class="row">

            <dt class="col-sm-2">
                @* @Html.DisplayNameFor(model => model.Name)*@Ad
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt class="col-sm-2">
                @*@Html.DisplayNameFor(model => model.Description)*@Rol Detayı

            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Description)
            </dd>
            <dt class="col-sm-2">
                @*@Html.DisplayNameFor(model => model.IsActive)*@Aktif Rol

            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.IsActive)
            </dd>


        </dl>
        <form asp-action="DeleteRole">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Sil" class="btn btn-danger" /> |
            <a asp-action="Index">Rol Listesine Geri Dön</a>
        </form>
    </div>
</body>
</html>